# Rust
/target
**/*.rs.bk
# Cargo.lock currently ignored. Once the build is verified end-to-end on
# Rust ≥ 1.85, remove this line and commit the lockfile — we publish bins
# and reproducible builds matter for the v0.1 release-gate test.
Cargo.lock

# macOS
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Logs / scratch
*.log
/scratch/

# Plugin state (oh-my-claudecode etc.)
.omc/
.claude/local/

# cc-connect runtime state — do NOT commit personal identity / room logs
# (this directory is for users running cc-connect, not for the repo itself)
.cc-connect-spike/results-personal-*
